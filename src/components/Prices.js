import React from 'react';
import PropTypes from 'prop-types';

const Prices = props => {
    return (
        <div>
            
        </div>
    );
};

Prices.propTypes = {
    
};

export default Prices;